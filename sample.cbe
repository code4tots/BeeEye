(def draw (lambda (string)
	(clear)
	(def x 0)
	(def y 0)
	(def i 0)
	(def c '')

	(while (< i (len string))

		(let c ([] string i))
		(putch x y c)
		(let i (+ i 1))
	)

))

(def s '')

(while 1 (block

	(let s (strcat s (getkey)))

	(draw s)

))
